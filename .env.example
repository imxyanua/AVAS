# Copy to .env and fill in values. .env is gitignored.

# Google GenAI / Gemini API key for operator-facing incident reports.
# Leave empty to use the deterministic template report instead.
GOOGLE_API_KEY=

# Optional model override used by src.genai.analyzer.
GOOGLE_GENAI_MODEL=gemini-2.0-flash
